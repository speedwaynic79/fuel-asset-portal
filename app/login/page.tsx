'use client';import{useState}from'react';import{useRouter}from'next/navigation';import{sb}from'@/lib/supabase/client';export default function P(){const[e,se]=useState(''),[p,sp]=useState(''),[m,sm]=useState('');const r=useRouter();async function go(x:React.FormEvent){x.preventDefault();const{error}=await sb().auth.signInWithPassword({email:e,password:p});if(error)sm(error.message);else r.replace('/dashboard')}return <main className="wrap"><div className="panel" style={{maxWidth:430,margin:'60px auto'}}><h1>Fuel Asset Portal</h1><p className="muted">Inspect once. Record once. Manage for life.</p><form onSubmit={go}><input type="email" placeholder="Email" value={e} onChange={x=>se(x.target.value)} required/><input type="password" placeholder="Password" value={p} onChange={x=>sp(x.target.value)} required/><button>Sign in</button></form><p className="muted">{m}</p></div></main>}