import{redirect}from'next/navigation';import{revalidatePath}from'next/cache';import{sb}from'@/lib/supabase/server';async function create(f:FormData){'use server';const s=await sb();await s.from('actions').insert({asset_id:String(f.get('asset_id')),title:String(f.get('observation')),category:String(f.get('category')),priority:String(f.get('priority')),status:'OPEN'});revalidatePath('/technician')}export default async function P(){const s=await sb(),{data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const{data:a}=await s.from('assets').select('id,asset_code,asset_type');return <main className="wrap"><h1>Technician Field View</h1><div className="panel" style={{maxWidth:650}}><form action={create}><label>Asset</label><select name="asset_id">{(a||[]).map(x=><option key={x.id} value={x.id}>{x.asset_code} — {x.asset_type}</option>)}</select><label>Response</label><select defaultValue="DEFECT"><option>PASS</option><option>DEFECT</option><option>N/A</option><option>NOT VERIFIED</option></select><label>Observation</label><textarea name="observation" required/><label>Category</label><select name="category"><option>Asset condition</option><option>Safety critical</option><option>Compliance</option><option>Documentation</option></select><label>Prototype priority</label><select name="priority" defaultValue="Medium*"><option>Advisory*</option><option>Medium*</option><option>High*</option><option>Critical*</option></select><button>Create finding / action</button></form><p className="muted">Priority labels are illustrative until the production risk methodology is approved.</p></div></main>}